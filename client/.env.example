# Leave these blank in local development to use the built-in Vite proxy.
# Set them explicitly for deployed environments.
VITE_API_URL=
VITE_SERVER_URL=
VITE_GOOGLE_CLIENT_ID=
VITE_WEB_PUSH_PUBLIC_KEY=
